%p#notice= notice
%h1 Fibs
%table
  %thead
    %tr
      %th Iterations
      %th Result
      %th{:colspan => "3"}
  %tbody
    - fibs.each do |fib|
      %tr
        %td= fib.iterations
        %td= fib.to_s
        %td= link_to 'Show', fib
        %td= link_to 'Edit', edit_fib_path(fib)
        %td= link_to 'Destroy', fib, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Fib', new_fib_path
